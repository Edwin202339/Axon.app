// Este componente es un alias que redirige al componente original en la carpeta animations
import CosmicRiverAnimation from '../animations/CosmicRiver';

// Exportamos directamente para que se pueda usar desde ambas rutas
export default CosmicRiverAnimation;
